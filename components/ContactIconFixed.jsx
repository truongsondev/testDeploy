import React from 'react'

const ContactIconFixed = (props) => {
  return (
    <label className="contact-icon-fixed icon-messenger">
        <img src={props.src} alt="" />
    </label>
  )
}

export default ContactIconFixed